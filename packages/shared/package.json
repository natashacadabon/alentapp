{
    "name": "@alentapp/shared",
    "version": "1.0.0",
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\" && tsc --ignoreConfig --target ES2022 --module NodeNext --moduleResolution NodeNext --declaration --outDir dist index.ts"
    }
}
