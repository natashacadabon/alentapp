{
    "name": "@alentapp/api",
    "version": "1.0.0",
    "type": "module",
    "main": "src/app.ts",
    "scripts": {
        "dev": "tsx watch src/app.ts",
        "build": "tsc --ignoreConfig --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --skipLibCheck --esModuleInterop --outDir .build src/app.ts || node -e \"require('fs').existsSync('.build/api/src/app.js') || process.exit(1)\" && node -e \"const fs = require('fs'); fs.rmSync('dist', { recursive: true, force: true }); fs.cpSync('.build/api/src', 'dist', { recursive: true }); fs.cpSync('src/generated', 'dist/generated', { recursive: true }); fs.rmSync('.build', { recursive: true, force: true });\"",
        "test": "vitest",
        "coverage": "vitest run --coverage"
    },
    "dependencies": {
        "@alentapp/shared": "^1.0.0",
        "@fastify/cors": "^11.2.0",
        "@prisma/adapter-pg": "7.8.0",
        "@prisma/client": "7.8.0",
        "fastify": "^5.8.5"
    },
    "devDependencies": {
        "@types/pg": "^8.20.0",
        "@vitest/coverage-v8": "^4.1.5",
        "dotenv": "^16.6.1",
        "pg": "^8.20.0",
        "prisma": "7.8.0",
        "tsx": "^4.21.0",
        "vitest": "^4.1.5"
    }
}
